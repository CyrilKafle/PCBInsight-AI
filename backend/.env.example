# Copy this file to `.env` (same folder) and fill in your key.
# `.env` is gitignored and never committed.
#
# Only needed for the AI layer (the `--ai` CLI flag, the /api/chat endpoint,
# and the "generate a Claude narrative review" dashboard checkbox). The
# deterministic analysis, scoring, HTML/PDF reports, and the dashboard all
# work without it.
ANTHROPIC_API_KEY=
